<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permission to query all packages on Android 11+ -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <application
        android:name=".BlindSpotApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Blindspot">

        <!-- 1. Tells LSPosed this is a module -->
        <meta-data
            android:name="xposedmodule"
            android:value="true" />

        <!-- 2. This is the description users see in the LSPosed Manager -->
        <meta-data
            android:name="xposeddescription"
            android:value="Hardening: Blocks Messenger Search to prevent relapse." />

        <!-- 3. Matches the version of the Xposed API we used in build.gradle -->
        <meta-data
            android:name="xposedminversion"
            android:value="82" />

        <!-- Main Activity with launcher icon -->
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:exported="true"
            android:enabled="true"
            android:persistent="true">
            <intent-filter android:priority="999">
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- App Protection Activity -->
        <activity
            android:name=".AppProtectionActivity"
            android:label="App Protection"
            android:exported="false" />

        <receiver
            android:name=".BlindSpotAdminReceiver"
            android:label="BlindSpot Manager"
            android:description="@string/app_name"
            android:permission="android.permission.BIND_DEVICE_ADMIN"
            android:exported="true">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin_rules" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>